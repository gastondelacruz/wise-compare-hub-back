# Common

Technical utilities and infrastructure-level constants.

## Structure

- `constants/` - Technical constants (pagination, timeouts, rate limits)
- `decorators/` - Custom decorators
- `pipes/` - Custom pipes
- `validators/` - Custom validators

## Rules

- Technical/infrastructure constants only
- Business rule constants go in domain layer of each context
- Can be imported by any layer
